---
title: A post in progress
pubDate: 2026-07-20
description: This one is still a draft, so it should not appear in the writing index or build its own page.
draft: true
---

This post has `draft: true` in its front matter, which means it is deliberately hidden: it
does not show up in the writing list, it doesn't get its own page in the built site, and it
won't be picked up by search engines.

It's here as a live example of the draft workflow. Flip `draft` to `false` (and delete this
note) when a post is ready to publish.
